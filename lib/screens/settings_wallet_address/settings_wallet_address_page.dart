import 'package:flutter/material.dart';
import 'package:realunit_wallet/screens/receive/receive_page.dart';

class SettingsWalletAddressPage extends StatelessWidget {
  const SettingsWalletAddressPage({super.key});

  @override
  Widget build(BuildContext context) => const ReceivePage(isBottomSheet: false);
}
